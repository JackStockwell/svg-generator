const { Circle } = require("./shapes")

describe('SVG Class Generator', () => {

    describe('Circle Class', () => {
        it('Will return a circle SVG String', () => {
            const shape = new Circle();
            shape.setColor
        })
    })
})